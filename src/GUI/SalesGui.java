package GUI;
/*
import java.awt.BorderLayout;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JList;
import javax.swing.JTextField;
import javax.swing.event.ListSelectionEvent;
import javax.swing.event.ListSelectionListener;

class SalesGui extends JFrame{
	private static final String[] OPTIONS = {"Register food order", "Change good order", "Register customer information",
												"Change customer information", "View customer list"};
	private static final String EXTRA_OPTION = "Register package deal";
	private JList list_options = new JList(OPTIONS);
	
	
	public SalesGui(String title){
		setTitle(title);
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		JLabel text = new JLabel("Choose one of the following options: ");
		add(text, BorderLayout.NORTH);
		
		Buttonlistener listener = new Buttonlistener();
		list_options.addListSelectionListener(listener);
		
		pack();
	}
	private class Buttonlistener implements ListSelectionListener {
		public void actionPerformed(ListSelectionEvent event) {
		}
	}
}*/
